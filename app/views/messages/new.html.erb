<div class="msg">
  <% @messages.each do |msg| %>
    <p><%= msg.content %></p>
  <% end %>
</div>

<%= form_for @message, remote: true do |f| %>
  <%= f.text_field :content %>
  <%= f.submit %>
<% end %>